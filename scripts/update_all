#! /usr/bin/env nix-shell
#! nix-shell -i fish -p niv

set -l root (git rev-parse --show-toplevel 2> /dev/null)

for f in **/sources.json
    set -l dir (dirname (dirname $f))
    echo "Updating sources for $dir"
    cd $dir
    niv update
    cd $root
end


# vim:ft=fish
